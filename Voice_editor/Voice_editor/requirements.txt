# Core Build Tools (Must be at the top)
setuptools==70.0.0
wheel
# NumPy 1.26.4 is the most stable version for Python 3.11
numpy==1.26.4

# AI & Voice Engines (Optimized for Railway's CPU RAM)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
torchaudio==2.4.1+cpu
# TTS 0.22.0 is the last stable version with full feature support
TTS==0.22.0
onnxruntime==1.19.0

# API & Web Framework
fastapi==0.115.0
uvicorn==0.30.1
python-multipart==0.0.9
python-dotenv==1.2.2

# Audio Processing Helpers
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
huggingface_hub
